Known failure modes:

- user pays but machine does not start
- machine starts but app does not reflect it
- duplicate payment on retry
- stale machine availability
- timer desync after app background
